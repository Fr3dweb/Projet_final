<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>
        {% block title %}Entreprise Longo Formation
        {% endblock %}
    </title>
    <meta name="viewport" content="device-width, initial-scale=1"/>
    <script src="https://kit.fontawesome.com/792c5c3f5a.js" crossorigin="anonymous"></script>
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

</head>
<body>
{% include('partials/header.html.twig') %}
{% block myContent %}
    <div class="container">{#     {% for message in app.flashes('success') %} #}
        {#        <div class="d-flex justify-content-center"> #}
        {#        <div class=" alert alert-success position-absolute shadow-lg mb-0 mt-4 p-5"> #}
        {#         {{ message }} #}
        {#        </div> #}
        {#        </div> #}
        {#     {% endfor %} #}
    </div>
{% endblock %}
{% include('partials/footer.html.twig') %}
</body>
</html>
